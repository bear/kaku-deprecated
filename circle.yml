dependencies:
  override:
    - cd /home/ubuntu/.pyenv/plugins/python-build/../.. && git pull && cd -
    - pip install -U pip
    - make dev

test:
  pre:
    - lsb_release -a
  override:
    - make ci
